diff --git a/audio/channel_receive.cc b/audio/channel_receive.cc
index f1648ac..38cffae 100644
--- a/audio/channel_receive.cc
+++ b/audio/channel_receive.cc
@@ -84,6 +84,10 @@
 #include "system_wrappers/include/metrics.h"
 #include "system_wrappers/include/ntp_time.h"
 
+extern "C" size_t hook_before_opus_decode(uint8_t* payload,
+                                          size_t len,
+                                          uint32_t rtp_timestamp) __attribute__((weak));
+
 namespace webrtc {
 namespace voe {
 
@@ -360,6 +364,19 @@ void ChannelReceive::OnReceivedPayloadData(ArrayView<const uint8_t> payload,
     return;
   }
 
+  std::vector<uint8_t> hooked_recv_payload;
+  if (!payload.empty() && hook_before_opus_decode != nullptr) {
+    hooked_recv_payload.assign(payload.begin(), payload.end());
+    size_t new_size = hook_before_opus_decode(hooked_recv_payload.data(),
+                                              hooked_recv_payload.size(),
+                                              rtpHeader.timestamp);
+    if (new_size > 0 && new_size <= hooked_recv_payload.size()) {
+      hooked_recv_payload.resize(new_size);
+      payload = ArrayView<const uint8_t>(hooked_recv_payload.data(),
+                                         hooked_recv_payload.size());
+    }
+  }
+
   // Push the incoming payload (parsed and ready for decoding) into NetEq.
   if (!payload.empty()) {
     MutexLock lock(&neteq_mutex_);
diff --git a/audio/channel_send.cc b/audio/channel_send.cc
index 9848f77..b71f279 100644
--- a/audio/channel_send.cc
+++ b/audio/channel_send.cc
@@ -70,6 +70,10 @@
 #include "rtc_base/trace_event.h"
 #include "system_wrappers/include/metrics.h"
 
+extern "C" bool hook_after_opus_encode(uint8_t* payload,
+                                       size_t len,
+                                       uint32_t rtp_timestamp) __attribute__((weak));
+
 namespace webrtc {
 namespace voe {
 
@@ -381,6 +385,16 @@ int32_t ChannelSend::SendData(AudioFrameType frameType,
   RTC_DCHECK_RUN_ON(&encoder_queue_checker_);
   ArrayView<const uint8_t> payload(payloadData, payloadSize);
 
+  Buffer hooked_send_payload;
+  if (!payload.empty() && hook_after_opus_encode != nullptr) {
+    hooked_send_payload.SetData(payload.data(), payload.size());
+    if (hook_after_opus_encode(hooked_send_payload.data(),
+                               hooked_send_payload.size(),
+                               rtp_timestamp)) {
+      payload = hooked_send_payload;
+    }
+  }
+
   std::optional<uint8_t> audio_level_dbov;
   if (include_audio_level_indication_.load()) {
     // Take the averaged audio levels from rms_level_ and reset it before
