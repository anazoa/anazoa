diff --git a/webrtc-sys/Cargo.toml b/webrtc-sys/Cargo.toml
index 0dc40648..1e5c4d51 100644
--- a/webrtc-sys/Cargo.toml
+++ b/webrtc-sys/Cargo.toml
@@ -1,28 +1,28 @@
 [package]
 name = "webrtc-sys"
 version = "0.3.43"
-edition.workspace = true
+edition = "2021"
 homepage = "https://livekit.io"
-license.workspace = true
+license = "Apache-2.0"
 description = "Unsafe bindings to libwebrtc"
-repository.workspace = true
+repository = "https://github.com/livekit/rust-sdks"
 
 [features]
 default = []
 
 [dependencies]
 cxx = "1.0"
-log = { workspace = true }
+log = "0.4"
 
 [build-dependencies]
-webrtc-sys-build = { workspace = true }
+webrtc-sys-build = { path = "build" }
 cxx-build = "1.0"
 glob = "0.3"
-cc = { workspace = true }
+cc = { version = "1.0", features = ["parallel"] }
 pkg-config = "0.3.22"
 
 [target.'cfg(target_os = "linux")'.build-dependencies]
 pkg-config = "0.3.22"
 
 [dev-dependencies]
-env_logger = { workspace = true }
+env_logger = "0.11"
diff --git a/webrtc-sys/build.rs b/webrtc-sys/build.rs
index 6451740b..beba6d7c 100644
--- a/webrtc-sys/build.rs
+++ b/webrtc-sys/build.rs
@@ -23,7 +23,8 @@ fn main() {
 
     let target_os = env::var("CARGO_CFG_TARGET_OS").unwrap();
     let target_arch = env::var("CARGO_CFG_TARGET_ARCH").unwrap();
-    let is_desktop = target_os == "linux" || target_os == "windows" || target_os == "macos";
+    // let is_desktop = target_os == "linux" || target_os == "windows" || target_os == "macos";
+    let is_desktop = false;
 
     println!("cargo:rerun-if-env-changed=LK_DEBUG_WEBRTC");
     println!("cargo:rerun-if-env-changed=LK_CUSTOM_WEBRTC");
@@ -132,8 +133,6 @@ fn main() {
         builder.define(key.as_str(), value);
     }
 
-    // Link webrtc library
-    println!("cargo:rustc-link-lib=static=webrtc");
     match target_os.as_str() {
         "windows" => {
             println!("cargo:rustc-link-lib=dylib=msdmo");
@@ -178,14 +177,17 @@ fn main() {
 
             configure_hermetic_libcxx(&mut builder, &webrtc_include);
 
-            // In order to avoid any ABI mismatches we use the sysroot's headers.
-            add_gio_headers(&mut builder);
+            if is_desktop {
+                // In order to avoid any ABI mismatches we use the sysroot's headers.
+                add_gio_headers(&mut builder);
 
-            // Do not use pkg_config::probe_library, because we only require headers.
-            for lib_name in ["glib-2.0", "gobject-2.0", "gio-2.0"] {
-                let lib = pkg_config::Config::new().cargo_metadata(false).probe(lib_name).unwrap();
-                for path in lib.include_paths {
-                    builder.include(path);
+                // Do not use pkg_config::probe_library, because we only require headers.
+                for lib_name in ["glib-2.0", "gobject-2.0", "gio-2.0"] {
+                    let lib =
+                        pkg_config::Config::new().cargo_metadata(false).probe(lib_name).unwrap();
+                    for path in lib.include_paths {
+                        builder.include(path);
+                    }
                 }
             }
 
@@ -372,7 +374,13 @@ fn main() {
                 .flag("-std=c++20");
         }
         "android" => {
-            webrtc_sys_build::configure_jni_symbols().unwrap();
+            // JNI symbol force-keep/version-script generation now happens in
+            // anazoa-tun's own build.rs, not here — webrtc-sys is only a
+            // dependency of the actual cdylib target (anazoa-tun), so
+            // emitting cargo:rustc-link-arg-cdylib from this crate relied on
+            // an unintended-but-kept-for-compatibility Cargo propagation
+            // quirk (https://github.com/rust-lang/cargo/issues/9562) instead
+            // of applying to the crate it actually describes.
 
             println!("cargo:rustc-link-lib=EGL");
             println!("cargo:rustc-link-lib=OpenSLES");
@@ -396,6 +404,10 @@ fn main() {
     builder.define("LIVEKIT_TEST", None);
     builder.warnings(false).compile("webrtcsys-cxx");
 
+    // Link webrtc after webrtcsys-cxx so GNU ld sees the undefined references
+    // from webrtcsys-cxx before scanning the archive, pulling in the right objects.
+    println!("cargo:rustc-link-lib=static=webrtc");
+
     for entry in glob::glob("./src/**/*.cpp").unwrap() {
         println!("cargo:rerun-if-changed={}", entry.unwrap().display());
     }
diff --git a/webrtc-sys/build/Cargo.toml b/webrtc-sys/build/Cargo.toml
index 4e985b52..d3abf22b 100644
--- a/webrtc-sys/build/Cargo.toml
+++ b/webrtc-sys/build/Cargo.toml
@@ -1,10 +1,10 @@
 [package]
 name = "webrtc-sys-build"
 version = "0.3.19"
-edition.workspace = true
-license.workspace = true
+edition = "2021"
+license = "Apache-2.0"
 description = "Build utilities when working with libwebrtc"
-repository.workspace = true
+repository = "https://github.com/livekit/rust-sdks"
 
 [dependencies]
 reqwest = { version = "0.12", default-features = false, features = ["rustls-tls-native-roots", "blocking"] }
@@ -13,4 +13,4 @@ regex = "1.0"
 scratch = "1.0"
 fs2 = "0.4"
 semver = "1.0"
-anyhow = { workspace = true }
+anyhow = "1.0"
diff --git a/webrtc-sys/build/src/lib.rs b/webrtc-sys/build/src/lib.rs
index 586b6675..4aeb3a18 100644
--- a/webrtc-sys/build/src/lib.rs
+++ b/webrtc-sys/build/src/lib.rs
@@ -118,12 +118,13 @@ pub fn webrtc_dir() -> path::PathBuf {
 pub fn webrtc_defines() -> Vec<(String, Option<String>)> {
     // read preprocessor definitions from webrtc.ninja
     let defines_re = Regex::new(r"-D(\w+)(?:=([^\s]+))?").unwrap();
-    let mut files = vec![webrtc_dir().join("webrtc.ninja")];
-    // include desktop_capture.ninja to avoid ABI mismatch for DesktopCaptureOptions due to WEBRTC_USE_X11 missing
-    // libwebrtc does not implement desktop capture on Android
-    if env::var("CARGO_CFG_TARGET_OS").unwrap() != "android" {
-        files.push(webrtc_dir().join("desktop_capture.ninja"));
-    }
+    let files = vec![webrtc_dir().join("webrtc.ninja")];
+    // let mut files = vec![webrtc_dir().join("webrtc.ninja")];
+    // // include desktop_capture.ninja to avoid ABI mismatch for DesktopCaptureOptions due to WEBRTC_USE_X11 missing
+    // // libwebrtc does not implement desktop capture on Android
+    // if env::var("CARGO_CFG_TARGET_OS").unwrap() != "android" {
+    //     files.push(webrtc_dir().join("desktop_capture.ninja"));
+    // }
 
     let mut seen = HashSet::new();
     let mut vec = Vec::new();
@@ -178,9 +179,16 @@ pub fn configure_jni_symbols() -> Result<()> {
         return Err(anyhow!("No JNI symbols found")); // Shouldn't happen
     }
 
-    // Keep JNI symbols
+    // Keep JNI symbols. Plain `cargo:rustc-link-arg` only applies when the
+    // *emitting* crate is the one directly producing the final artifact —
+    // webrtc-sys is a dependency of anazoa-tun (the actual cdylib), so those
+    // directives were silently dropped: this build script's own log showed
+    // all 224 --undefined= lines plus --version-script, correctly computed,
+    // but none of it ever reached the real link command (verified via
+    // `cargo build -v`). `-cdylib` is the variant designed to propagate from
+    // anywhere in the dependency graph to the final cdylib/bin target.
     for symbol in &jni_symbols {
-        println!("cargo:rustc-link-arg=-Wl,--undefined={}", symbol);
+        println!("cargo:rustc-link-arg-cdylib=-Wl,--undefined={}", symbol);
     }
 
     // Version script
@@ -191,7 +199,7 @@ pub fn configure_jni_symbols() -> Result<()> {
     write!(vs_file, "JNI_WEBRTC {{\n\tglobal: {}; \n}};", jni_symbols)
         .context("Failed to write version script")?;
 
-    println!("cargo:rustc-link-arg=-Wl,--version-script={}", vs_path.display());
+    println!("cargo:rustc-link-arg-cdylib=-Wl,--version-script={}", vs_path.display());
 
     Ok(())
 }
diff --git a/webrtc-sys/libwebrtc/.gclient b/webrtc-sys/libwebrtc/.gclient
index 490388b9..f5f0033b 100644
--- a/webrtc-sys/libwebrtc/.gclient
+++ b/webrtc-sys/libwebrtc/.gclient
@@ -1,9 +1,10 @@
 solutions = [
   {
     "name": 'src',
-    "url": 'https://github.com/webrtc-sdk/webrtc.git@m150_release',
+    "url": 'https://github.com/webrtc-sdk/webrtc.git@030ad13afd0ab7d16c70662e0e793ea1b892c158',
     "custom_deps": {},
     "deps_file": "DEPS",
     "managed": False,
   },
 ]
+target_os = ['android', 'linux']
diff --git a/webrtc-sys/libwebrtc/build_linux.sh b/webrtc-sys/libwebrtc/build_linux.sh
index 417c9b9f..a78bba6a 100755
--- a/webrtc-sys/libwebrtc/build_linux.sh
+++ b/webrtc-sys/libwebrtc/build_linux.sh
@@ -79,6 +79,9 @@ git apply "$COMMAND_DIR/patches/fix_desktop_capture_compile.patch" -v --ignore-s
 git apply "$COMMAND_DIR/patches/external_audio_source.patch" -v --ignore-space-change --ignore-whitespace --whitespace=nowarn
 git apply "$COMMAND_DIR/patches/fix_pipewire_utils_compile.patch" -v --ignore-space-change --ignore-whitespace --whitespace=nowarn
 
+git apply "$COMMAND_DIR/patches/hooks-opus.patch" -v --ignore-space-change --ignore-whitespace --whitespace=nowarn
+git apply "$COMMAND_DIR/patches/hooks-vp9.patch" -v --ignore-space-change --ignore-whitespace --whitespace=nowarn
+
 # Disable CREL (compact relocations). Chromium's build enables experimental
 # CREL via -Wa,--crel which causes segfaults on aarch64-linux (and is known
 # broken on arm32 and s390x too).
@@ -136,12 +139,12 @@ args="is_debug=$debug  \
   enable_stripping=true \
   ffmpeg_branding=\"Chrome\" \
   rtc_use_h264=true \
-  rtc_use_h265=true \
-  rtc_use_pipewire=true \
+  rtc_use_h265=false \
+  rtc_use_pipewire=false \
   symbol_level=0 \
   enable_iterator_debugging=false \
   use_rtti=true \
-  rtc_use_x11=true"
+  rtc_use_x11=false"
 
 # generate ninja files
 gn gen "$OUTPUT_DIR" --root="src" --args="${args}"
@@ -209,7 +212,7 @@ vpython3 "./src/tools_webrtc/libs/generate_licenses.py" \
   --target :default "$OUTPUT_DIR" "$OUTPUT_DIR" || echo "Warning: License generation failed (non-critical)"
 
 cp "$OUTPUT_DIR/obj/webrtc.ninja" "$ARTIFACTS_DIR"
-cp "$OUTPUT_DIR/obj/modules/desktop_capture/desktop_capture.ninja" "$ARTIFACTS_DIR"
+# cp "$OUTPUT_DIR/obj/modules/desktop_capture/desktop_capture.ninja" "$ARTIFACTS_DIR"
 cp "$OUTPUT_DIR/args.gn" "$ARTIFACTS_DIR"
 
 cp "$OUTPUT_DIR/LICENSE.md" "$ARTIFACTS_DIR"
