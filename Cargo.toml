[workspace]
members = ["auth", "config", "ctl", "mock", "tun"]
resolver = "2"

[workspace.package]
version = "0.2.0"

[profile.release]
strip = true
lto = true
codegen-units = 1
panic = "abort"
opt-level = "z"
