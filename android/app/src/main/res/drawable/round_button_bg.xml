<?xml version="1.0" encoding="utf-8"?>
<!-- Plain oval; fill color is set at runtime via ViewCompat.setBackgroundTintList
     so one drawable can serve every connect/disconnect/connecting state. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@android:color/white" />
</shape>
